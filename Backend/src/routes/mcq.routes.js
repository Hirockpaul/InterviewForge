const express=require('express');const{authUser}=require('../middlewares/auth.middlewares');const controller=require('../controllers/mcq.controller');const router=express.Router();router.use(authUser);router.get('/topics',controller.topicsController);router.get('/pool-status',controller.poolStatusController);router.get('/history',controller.historyController);router.post('/sessions',controller.createSessionController);router.get('/sessions/:id',controller.getSessionController);router.post('/sessions/:id/answer',controller.submitAnswerController);router.post('/sessions/:id/complete',controller.completeController);router.get('/sessions/:id/result',controller.resultController);module.exports=router
