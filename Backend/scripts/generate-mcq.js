const path=require('path');require('dotenv').config({path:path.join(__dirname,'../.env'),quiet:true});const mongoose=require('mongoose');const{resolveTopic}=require('../src/services/focusedPractice.services');const{QuestionPoolService}=require('../src/services/mcqPool.services');async function run(){const input=process.argv[2],category=process.argv[3]||'programming';if(!input)throw new Error('Usage: npm run generate:mcq -- "JavaScript" programming');await mongoose.connect(process.env.MONGO_URI);const topic=resolveTopic(input),pool=new QuestionPoolService();console.log(await pool.replenish({canonicalTopic:topic.canonicalTopic,displayTopic:topic.displayTopic,category:resolveTopic(category).canonicalTopic}));await mongoose.disconnect()}run().catch(error=>{console.error(error.message);process.exit(1)})
